# En produccion (Vercel), dejar vacio: el rewrite /api/* apunta al mismo dominio.
VITE_API_BASE_URL=

# Solo para desarrollo local: proxea /api hacia el backend Django corriendo en local.
VITE_API_PROXY=http://localhost:8000
